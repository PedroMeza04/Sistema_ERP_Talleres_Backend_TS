// Carpeta y archivo nuevos. Son los mismos números (1, 2, 3) que ya se usaban sueltos
// por el código para el campo rol — nada más les pusimos nombre, para que se lea
// "rol === ROL_ADMIN" en vez de "rol === 1" y quede más claro qué significa cada uno.
export const ROL_ADMIN = 1;
export const ROL_MECANICO = 2;
export const ROL_RECEPCIONISTA = 3;
